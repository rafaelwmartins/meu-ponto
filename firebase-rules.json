{
    "rules": {
        "$id": {
            ".read": "(auth.provider == 'facebook' && $id == auth.id) || (auth.provider != 'facebook' && $id == auth.uid)",
            ".write": "(auth.provider == 'facebook' && $id == auth.id) || (auth.provider != 'facebook' && $id == auth.uid)"
        }
    }
}
